{
  "dependencies": {
    "@google/gemini-cli": "^0.28.2"
  },
  "scripts": {
    "build": "echo '--- VERCEL BUILD SCRIPT START ---' && npm run build:frontend && npm run build:backend && echo '--- VERCEL BUILD SCRIPT COMPLETE ---'",
    "build:frontend": "echo '--- Building frontend ---' && cd frontend && npm install && npm run build && cd .. && echo '--- Moving frontend output to /public ---' && mv frontend/dist public",
    "build:backend": "node scripts/build-models.js"
  }
}
